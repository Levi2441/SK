const Instructions = () => {
  return (
    <div>
      <h1 className="MainInstructionsHeader">Instructions</h1>
      <ol>
        <li className="MainInstructionsInfo">
          1. Enter in only the name of a product into the three boxes (if
          product is not in database, fill out the form below to request it to
          be added)
        </li>
        <li className="MainInstructionsInfo">
          2. Press submit when all names are entered
        </li>
      </ol>
    </div>
  );
};

export default Instructions;
